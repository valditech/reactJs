import styles from "./style.module.css"

export const Footer = () =>{
    return(
        <footer className={styles.footer}>
            <p className="paragraph">Tout droit reserver a -V.N.</p>
        </footer>
    )
}